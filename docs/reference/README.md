# Reference source documents — Zephyr COSS

These are the **original design documents** the demo example is derived from,
kept for reference. The machine-readable versions the tool actually consumes
live in `data/has/`, `data/specs/`, and `data/rtl/`.

| File | What it is |
| --- | --- |
| `Zephyr_COSS_HAS_WIP.docx` | High-level Architecture Spec (source) |
| `Zephyr_COSS_MAS_WIP.docx` | Micro-Architecture Spec (source) |
| `COSS_HAS_Diagrams.drawio` | HAS block/interface diagrams |
| `COSS_MAS_Diagrams.drawio` | MAS datapath / pipeline / FSM diagrams |

All content is synthetic and contains no proprietary or confidential material.
